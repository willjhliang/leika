# Contributing to Leika

See [docs/development.md](docs/development.md) for setup, the browser build,
and the checks to run before opening a change.

New features should stay inside Leika's data-workspace scope: panes, GUI,
layout, and transport for 2D visualizations. The base install should stay free
of any mandatory visualization-library dependency.
