Server
======

.. currentmodule:: leika

The server owns the websocket transport and the HTTP route that serves the
browser client. It exposes the page, pane, and GUI APIs as attributes.

.. autoclass:: Server
   :members:

.. autoclass:: ClientHandle
   :members:
